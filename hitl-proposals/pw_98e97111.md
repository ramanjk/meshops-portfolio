# HITL write proposal `pw_98e97111`

**Intent:** scale Deployment/demo-web in ns/meshops-workloads to 3 replica(s)

**Rationale:** Increase replicas from 1 to 3 for higher throughput, as requested by the user.

> Merging this PR **approves** the write; closing it **rejects** it. The steward's deterministic executor applies it in-process under its own bounded credentials (ADR-0011) — the PR is the approval signal, not the actuator.

## Dry-run preview

```
Deployment/demo-web in ns/meshops-workloads: replicas 1 -> 3. No change made (dry-run).
```

## Proposal

```json
{
  "kind": "Deployment",
  "namespace": "meshops-workloads",
  "name": "demo-web",
  "replicas": 3
}
```
