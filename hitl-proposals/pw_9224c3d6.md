# HITL write proposal `pw_9224c3d6`

**Intent:** create Pod in ns/meshops-workloads

**Rationale:** Creating a small diagnostic test Pod to verify write access for the user.

> Merging this PR **approves** the write; closing it **rejects** it. The steward's deterministic executor applies it under a namespaced, write-but-bounded ServiceAccount (ADR-0011).

## Server dry-run preview

```
pod/steward-b-livetest
```

## Proposal

```json
{
  "operation": "create",
  "resource_kind": "Pod",
  "namespace": "meshops-workloads",
  "name": null,
  "manifest": {
    "apiVersion": "v1",
    "kind": "Pod",
    "metadata": {
      "name": "steward-b-livetest",
      "namespace": "meshops-workloads",
      "labels": {
        "app": "steward-b-livetest",
        "meshops.io/ephemeral": "true"
      }
    },
    "spec": {
      "containers": [
        {
          "name": "busybox",
          "image": "busybox",
          "command": [
            "sleep",
            "600"
          ]
        }
      ],
      "restartPolicy": "Never",
      "activeDeadlineSeconds": 600
    }
  },
  "patch": null,
  "replicas": null
}
```
