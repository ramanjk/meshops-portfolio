apiVersion: azmonitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: hello-quality
  namespace: {{ .Values.namespace }}
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: hello-quality
  podMetricsEndpoints:
    - port: metrics
      interval: 30s
      path: /metrics
